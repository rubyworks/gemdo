= Project Class

Almost all use of Rock will be via the Project class. This class encapsulates
all the other aspects of the system from metdata to standard project paths.

  require 'rock/project'

  Rock::Project.new('tmp/example')

